string writeMessages() {
	string recieverName;
	bool found = true;
	char readOrWrite;
	while (found == true) {
		cout << "Who do you want to send the message to? ";
		cin >> recieverName;
		if (check(recieverName) == true) {
			cout << "User account found\n";
			found = false;
		}
		else {
			cout << "User account not found, please try again\n";
		}
	}
	
	return recieverName;
}

string readMessages() {
	string recieverName;
	bool found = true;
	char readOrWrite;
	cout << "Whose message do you want to read? ";
	cin >> recieverName;
	while (found == true) {
		if (check(recieverName) == true) {
			cout << "User account found\n";
			found = false;
		}
		else {
			cout << "User account not found, please try again\n";
		}
	}
	
	return recieverName;
}

/*Read Receipts*/
//Maybe write a another database function to count the amount of messages
//The database function can take one argument, this can be just the username
int read_receipts(senderName){
	sqlite3* db;
	char* errMsg = 0;
	int rc;
	sqlite3_stmt* stmt = 0;
	
	rc = sqlite3_open("Messages.db", &db);
	//if statment to check if database opens
	//maybe use int sqlite3_column_count(stmt); // This should return the number of columns in the result
	
}